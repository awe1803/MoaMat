# =============================================================================
#  Variables locales pour `supabase functions serve` (copier en .env.local et
#  NE JAMAIS commiter). En production, ces valeurs sont injectées
#  automatiquement par la plateforme Supabase — rien à configurer.
#
#  SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY sont fournis
#  d'office par le runtime Edge : ne les redéfinir ici que pour un test local
#  pointant vers un projet distant.
# =============================================================================

# SUPABASE_URL=https://<ref>.supabase.co
# SUPABASE_ANON_KEY=<clé anon / publishable>
# SUPABASE_SERVICE_ROLE_KEY=<clé service_role — secrète>

# notify-pending-account (notifications push) — à pousser en production avec
# `supabase secrets set`. Génération des clés VAPID : `npx web-push generate-vapid-keys`.
# PUSH_WEBHOOK_SECRET=<longue valeur aléatoire, identique au secret Vault moamat_notify_pending_account_secret>
# VAPID_PUBLIC_KEY=<clé publique VAPID, base64url — aussi dans Push:VapidPublicKey côté client>
# VAPID_PRIVATE_KEY=<clé privée VAPID, base64url — secrète, jamais côté client>
# VAPID_SUBJECT=mailto:<adresse de contact>
